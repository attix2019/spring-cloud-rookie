server:
  port: 18761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false

logging:
  level:
    com:
      netflix.eureka: OFF
      netflix.discovery: OFF


